@{
    ViewBag.Title = "Błąd Kalkulatora";
    Layout = "_Layout";
}

<div class="container">
    <div class="row justify-content-md-center">
        <div class="col-6">
            <h2>@ViewBag.Title</h2>
            <div class="alert alert-danger">
                <h3>@ViewBag.ErrorMessage</h3>
            </div>
            <a href="@Url.Action("Form", "Calculator")" class="btn btn-primary">Wróć do kalkulatora</a>
        </div>
    </div>
</div>